# veriflow-rs
A high-performance, asynchronous file transfer CLI built with Rust, Tokio and SHA-256 integrity verification.

## Structure

- **`server/`**: Contains server files.
        
- **`client/`**: Contains client files, more info via `veriflow -h`
