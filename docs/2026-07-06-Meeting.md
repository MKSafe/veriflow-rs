# **Meeting: MVP Final Sprint**

**Date: 06/07/2026**  
**Attendees: Both developers present**

# Meeting Notes

### V1 Action Items (MVP) from last meeting reviewed and revised

- ~~Patch Giant Header Exploit~~
    - ~~limit incoming JSON header size to prevent out-of-memory issues~~
- ~~Patch Path Traversal Vulnerability (sanitise filenames from clients)~~
- ~~Audit and remove `.unwrap()` calls to prevent server crashes - utilise `?` operator~~
- ~~Implement config files for both Client and Server~~
    - ~~Look into YAML as a potential file~~ - We chose native rust (.toml)
    - ~~Update the Server CLI to support specifying the resource dir via config~~
    - ~~Update the Client CLI to support specifying the IP, resource, and download folder~~
    - ~~Add config file to .gitignore to prevent leaking local settings/IP~~
- ~~Implement List command (server-side responds to client-side request)~~
- ~~Implement Delete Command~~
- Final codebase polish incl. cleaning redundancy
- Implement baseline unit tests:
    - client: done
    - server: partly done
        - \`safe_path\` testing
    - common: partly done:
        - Add protocol unit tests
            - (refactor from server/src/lib.rs)
            - security tests like giant header exploit
        - server/client merge config tests

Extras (TODO for MVP):

- merge & refactor server/client config files to common
- list for specific directories: --list &lt;OPTION&gt;
- Functionality for uploading and downloading entire directories
- .sha256 sidecar for server: `filename.txt` --> `filename.txt.sha256`
- Tests
    - Implement integration and e2e tests
    - Test coverage should be above 80%
- Write README.md

Extras (done):

- client config optional CLI which outputs specified config setting to console
- setup workspace for integration and e2e tests including integration list test

&nbsp;

### V2 Action Items (Future)

The following items must be complete for an official V2 (to be amended as needed).

- Implement User IDs (UID). Users will be able to authenticate via their own .pem key. Public keys stores on the Server DB.
- User Isolation by refactoring the server resource directly to dynamically create isolated sub-directories mapped ot each user's UID. Mapping done via DB.
- Implement transfer encryption
- Improve buffer management and overall transfer efficiency.
- Explore data partitioning
- Add comprehensive DB testing and new CLI commands (e.g. checking available server data)
- "Never Trust The Client" Philosophy (exploit fixing):
    - Disk exhaustion, check max size / available space (inc. race conditions)
    - UID with .pem for user authentication
    - DOS - Rate Limiting per IP address
    - Replay Attacks - add challenge response by sending a random nonce

&nbsp;

### Next Meeting

Trigger: To be scheduled upon the completion of all V1 action items.  
Agenda: Final V1 testing and establish official V2 kick-off timeline.